benutzen "http";

fun handler(req) {
    print req.method;
    print req.path;

    if (req.path == "/") {
        return http::serveFile(200, "index.html");
    }

    return http::response(404, "not found");
}

http::serve(8080, handler);
